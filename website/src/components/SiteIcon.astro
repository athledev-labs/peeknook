---
// SPDX-License-Identifier: Apache-2.0
import { siteIconSvg, type SiteIconName } from "../lib/siteIcons";

interface Props {
  name: SiteIconName;
}

const { name } = Astro.props;
---

<Fragment set:html={siteIconSvg[name]} />
